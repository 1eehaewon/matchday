<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <!-- find.xml -->
 
 <mapper namespace="find">
   <select id="findIdByNameAndEmail" resultType="String" parameterType="map">
        SELECT userID
        FROM users
        WHERE name = #{name} AND email = #{email}
    </select>
    
    
    <!-- 아이디로 이메일 찾기 -->
    <select id="findEmailByUserID" parameterType="string" resultType="string">
        SELECT email FROM users WHERE userid = #{userID}
    </select>

    <!-- 비밀번호 재설정 토큰 저장 -->
    <insert id="savePasswordResetToken">
        INSERT INTO password_reset_tokens (userID, token, expiration)
        VALUES (#{userID}, #{token}, NOW() + INTERVAL 1 HOUR)
    </insert>

    <!-- 토큰으로 아이디 찾기 -->
    <select id="findUserIDByToken" parameterType="string" resultType="string">
        SELECT userid FROM password_reset_tokens WHERE token = #{token}
    </select>

    <!-- 비밀번호 업데이트 -->
    <update id="updatePassword">
        UPDATE users SET password = #{password} WHERE userid = #{userID}
    </update>
</mapper>