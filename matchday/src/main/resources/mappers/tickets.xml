<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tickets">

	<select id="getMatchById" parameterType="String" resultType="kr.co.matchday.matches.MatchesDTO">
        SELECT matchid, hometeamid, awayteamid, stadiumid, matchdate, matchtime, referee, bookingstartdate, bookingenddate
        FROM matches
        WHERE matchid = #{matchid}
    </select>

</mapper>