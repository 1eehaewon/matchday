<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="matches">
    <!-- 경기 일정 등록 INSERT 쿼리 -->
    <insert id="saveMatch" parameterType="kr.co.matchday.matches.MatchesDTO">
        INSERT INTO matches (matchdate, matchtime, hometeamid, awayteamid, stadiumid)
        VALUES (#{matchdate}, #{matchtime}, #{hometeamid}, #{awayteamid}, #{stadiumid})
    </insert>

    <!-- 경기 일정 목록 조회 SELECT 쿼리 -->
    <select id="list" resultType="kr.co.matchday.matches.MatchesDTO">
        SELECT matchdate, matchtime, hometeamid, awayteamid, stadiumid
        FROM matches
    </select>
</mapper>