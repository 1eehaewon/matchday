<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="vcomment">

    <insert id="insert" parameterType="kr.co.matchday.comment.CommentDTO">
        INSERT INTO vcomment(cno, video_code, content, wname)
        VALUES (DEFAULT, #{video_code}, #{content}, #{wname})
    </insert>

    <select id="list" parameterType="Integer" resultType="kr.co.matchday.comment.CommentDTO">
        SELECT cno, video_code, content, wname, regdate 
        FROM vcomment 
        WHERE video_code = #{video_code}
        ORDER BY regdate
    </select>

  <update id="updateproc" parameterType="kr.co.matchday.comment.CommentDTO">
        UPDATE vcomment
        SET content = #{content}
        WHERE cno = #{cno}
    </update>
    
     <delete id="delete" parameterType="kr.co.matchday.comment.CommentDTO">
        DELETE FROM vcomment 
        WHERE cno = #{cno}
    </delete>
    
 

</mapper>